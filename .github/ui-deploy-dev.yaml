apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mol-view-stories-ui-ingress-dev
  namespace: mol-view-stories-ns
  labels:
    app: mol-view-stories-dev-ui
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.org/client-max-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-set-headers: "ingress-nginx/custom-headers"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  rules:
    - host: mol-view-stories-ui-dev.dyn.cloud.e-infra.cz
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: mol-view-stories-ui-svc-dev
                port:
                  number: 80
  tls:
    - hosts:
        - mol-view-stories-ui-dev.dyn.cloud.e-infra.cz
      secretName: mol-view-stories-ui-tls-dev
---
apiVersion: v1
kind: Service
metadata:
  name: mol-view-stories-ui-svc-dev
  namespace: mol-view-stories-ns
  labels:
    app: mol-view-stories-dev-ui
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: mol-view-stories-dev-ui
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mol-view-stories-dev-ui
  namespace: mol-view-stories-ns
  labels:
    app: mol-view-stories-dev-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mol-view-stories-dev-ui
  template:
    metadata:
      labels:
        app: mol-view-stories-dev-ui
    spec:
      imagePullSecrets:
        - name: cerit-registry-secret
      containers:
        - name: mol-view-stories-dev-ui-container
          image: cerit.io/mol-view-stories/mvs-ui:latest
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ENVIRONMENT
              value: "development"
            - name: NEXT_PUBLIC_APP_PREFIX
              value: "mol-view-stories"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            runAsUser: 101
            runAsGroup: 101
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          resources:
            requests:
              memory: "2Gi"
              cpu: "100m"
            limits:
              memory: "4Gi"
              cpu: "1"